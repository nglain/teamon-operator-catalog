# Third-party notices

The public Operator package includes third-party Node.js dependencies under
`plugins/teamon-operator/node_modules/`. Those components remain governed by
their own licenses, not the TeamON license at the root of this repository.

Each dependency's `package.json` identifies its name, version and license metadata.
The accompanying `LICENSE`, `COPYING`, `NOTICE` or similarly named files contain
the applicable terms and attribution where provided. Preserve those files when
copying a dependency. Transitive dependencies have their own terms as well.

The private Operator runtime is a separate delivery. These notices describe the
dependencies shipped in this public catalog and do not grant rights to that runtime.
